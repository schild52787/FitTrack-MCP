# Core MCP dependencies
mcp>=1.0.0,<2.0.0
fastmcp>=2.12.0,<3.0.0

# Data validation
pydantic>=2.8.0,<3.0.0

# HTTP client
httpx>=0.27.0,<1.0.0

# ASGI server (for HTTP/SSE deployment)
uvicorn[standard]>=0.30.0,<1.0.0

# Optional: for better logging
python-json-logger>=2.0.7,<3.0.0
